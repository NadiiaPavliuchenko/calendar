{
  "name": "calendar",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/calendar/",
    "preview": "vite preview",
    "watch": "npx vite build --watch"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
